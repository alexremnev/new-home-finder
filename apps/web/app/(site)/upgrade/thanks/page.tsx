export default function Page() {
  return (
    <main>
      <h1 style={{ fontSize: "1.4rem" }}>Payment received</h1>
      <p>
        Your plan is extended. Send <em>/show</em> to the bot to see the new limits — if it
        still shows the old plan, give it a minute and ask again.
      </p>
    </main>
  );
}
